package com.example.mp_sem_6;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.widget.TextView;

public class dashboard extends AppCompatActivity
{
    @Override
    protected void onCreate(Bundle savedInstanceState)
    {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.dashboard);

//        Intent intent = getIntent();
//        String message_signup = intent.getStringExtra(signup.MSG);
//        String message_login = intent.getStringExtra(login.MSG);
//
//        TextView textView = findViewById(R.id.dashboard);
//        textView.setText(message_signup);
//        textView.setText(message_login);
    }
}